import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spinandgold.trainer',
  appName: 'Spin & Gold Trainer',
  webDir: 'dist',
};

export default config;
